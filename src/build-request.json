{
  "kind": "build_request",
  "title": "Stripe card payment at cart checkout with post-success order creation",
  "projectName": "Card Checkout",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Enable Stripe card payment checkout from the Store cart by creating a Stripe Checkout Session and redirecting the user to Stripe for payment (do not treat a local form submit as payment success).",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "strip payment with card at checkout"
        ]
      },
      "acceptanceCriteria": [
        "From the cart drawer, selecting “Card Payment” initiates a Stripe Checkout Session creation via the backend and redirects the browser to Stripe Checkout.",
        "The card payment path does not call the existing order creation logic as a substitute for payment processing.",
        "If Stripe is not configured for the active mode, the UI shows an error state and does not attempt to start checkout."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Create and store the order only after Stripe payment is confirmed as successful, using the Stripe session status as the source of truth.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-2",
          "msg-3"
        ],
        "quotes": [
          "and try again, order after payment is successfull on stripe",
          "1 yes 2 yes"
        ]
      },
      "acceptanceCriteria": [
        "A Stripe-based order is not persisted as completed before the Stripe session is verified as paid/successful.",
        "On the payment success return route, the frontend reads the Stripe session identifier from the URL and calls the backend to verify session status with Stripe.",
        "Only after successful verification, the backend persists the order and marks it as completed; failed/canceled/unpaid sessions do not create a completed order.",
        "The payment failure route is shown when Stripe verification fails or indicates non-success status."
      ]
    },
    {
      "id": "REQ-3",
      "text": "Fix the current “Card Payment” flow so it no longer calls savePurchaseOrder directly on submit; instead it must collect buyer details, start Stripe checkout, and defer order creation until payment success verification completes.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1",
          "msg-2"
        ],
        "quotes": [
          "strip payment with card at checkout",
          "order after payment is successfull on stripe"
        ]
      },
      "acceptanceCriteria": [
        "Submitting the card checkout form does not clear the cart immediately and does not show “Order placed successfully!” until after Stripe success verification completes.",
        "Buyer details entered for card checkout are preserved across the Stripe redirect and are available when creating the order after payment success (e.g., stored in session/local storage or passed safely in the flow).",
        "After payment success verification completes and the order is created, the cart is cleared and a success toast/confirmation is shown."
      ]
    },
    {
      "id": "REQ-4",
      "text": "Restore and ensure persistence of Stripe settings (test secret key, live secret key, and active mode) in the backend canister, and ensure the Admin > Payments settings UI correctly loads the existing configuration and can update it.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-4",
          "msg-5"
        ],
        "quotes": [
          "also bring back stripe settings iv set up before",
          "yes the keys"
        ]
      },
      "acceptanceCriteria": [
        "Admin users can view Stripe configuration status (including last-4 masked display) and toggle active mode between Test and Live.",
        "Admin users can save/update test and/or live secret keys; the keys persist across reloads/upgrades when state is preserved.",
        "Non-admin users cannot read Stripe secret keys from any endpoint.",
        "Checkout session creation always uses the secret key corresponding to the currently active mode."
      ]
    },
    {
      "id": "REQ-5",
      "text": "Implement token-based discount and token earning in the Stripe card checkout flow: apply the automatic token discount during checkout calculation, and award bonus tokens only after payment success.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-2",
          "msg-3"
        ],
        "quotes": [
          "1 yes 2 yes",
          "order after payment is successfull on stripe"
        ]
      },
      "acceptanceCriteria": [
        "The Stripe checkout amount reflects any automatic token-based discount rules configured for the app (as described in project state).",
        "Bonus/earned tokens are recorded only after Stripe payment success is verified and the order is created.",
        "The Payment Success page reflects the finalized purchase and triggers refresh of token balance/transaction history."
      ]
    }
  ],
  "constraints": [
    "Backend must remain a single Motoko actor in backend/main.mo; do not introduce additional backend services.",
    "Do not edit frontend immutable paths: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui.",
    "Do not expose Stripe secret keys to non-admin users; only a public, non-secret Stripe config may be returned publicly.",
    "Do not use external databases or non-Internet-Computer backend technologies."
  ],
  "nonGoals": [
    "Implementing third-party authentication providers (only Internet Identity is in scope).",
    "Adding webhooks, external servers, or any off-canister backend to process Stripe events.",
    "Supporting non-card payment methods beyond existing token payment (e.g., Apple Pay-specific flows, ACH, Klarna)."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}