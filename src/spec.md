# Specification

## Summary
**Goal:** Publish the current draft build to production.

**Planned changes:**
- Deploy the latest draft version to the production environment.
- Verify the production URL loads successfully and matches the draft build.

**User-visible outcome:** Users can access the production URL and see the same app experience as the current draft build.
